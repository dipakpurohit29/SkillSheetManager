@{
	ViewData["Title"] = "Home Page";
}
<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="utf-8">
	<meta name="viewport" content="width=device-width, initial-scale=1">
	<title>Bootstrap 4 Responsive Layout</title>
	<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet">
	<script>
     const { Button } = require("../../wwwroot/lib/bootstrap/dist/js/bootstrap");
</script>
	<style>

	</style>
</head>
<body class="body-design btn-homepage" >
	<div class="container-xl pt-4 mb-4" style="height:100%">
		<h1>Skill Sheet Manager</h1>	
		<div>
			<form id="loginform-design" onsubmit = " return passwordValidationCheck();">
				<h1>Login</h1><br>
				<label class="label" for="uname"><b>GROUP NAME:</b></label>
				<select id="selectUserType" name="user" class="list1" onclick="clearValue()" required>
					<option value="Admin">Admin</option>
					<option value="User">User</option>
				</select><br><br>
				<label class="label" for="uname"><b>USER NAME :</b></label>
				<select name="user" id="selectPerson" class="list" onclick="getPersonName()" required>
				</select>
				<script type="text/javascript">
					function clearValue()
					                    {
						                  var names = document.getElementById("selectPerson");
						                  names.options.length = 0;
					                     }

			   	  function getPersonName()
					 {
						var list1 = document.getElementById('selectUserType');
						
					    var list2 = document.getElementById("selectPerson");
					    var list1SelectedValue = list1.options[list1.selectedIndex].value;																			
					    if (list1SelectedValue=='User')
													  {														 
															  var options = ["Dipak" , "prit" , "yash", "Khushali" , "priya"];
															  if(list2.length == 0){
																  
														      for(var i = 0; i < options.length; i++) 
														      {
										                               var opt = options[i];
										                               var element = document.createElement("option");
										                               element.textContent = opt;
										                               element.value = opt;
																	   list2.appendChild(element);										                             
									                         }	
															 }
					}
					else if (list1SelectedValue=='Admin')   
														{														
														 var options = ["Arti","Dipak", "Khushali"];
														  if(list2.length == 0){
														 for(var i = 0; i < options.length; i++) 
														 {
										                     var opt = options[i];
										                     var element = document.createElement("option");
										                     element.textContent = opt;
										                     element.value = opt;
										                     list2.appendChild(element);
									                     }
														  }
									                   }													  
				    }
				</script>
				<label class="label" for="password"><b>PASSWORD :</b></label>
				<input type="password" id="password" name="password" class="list" placeholder="Enter the password" required><br>				
				<input  type = "submit" value = "Login" class="login-btn" asp-controller="Home" asp-action="UserHomePage" >
				<input type="button" value="Reset" class="reset-btn" onClick="resetForm();">
				<script>
					function resetForm(){
						                 window.location.reload();
					                    }
                    function passwordValidationCheck(){
						                                 var pwd =  document.getElementById("password").value;  
						                               if(pwd.length < 8){
														                        alert("Error : Password length must be atleast 8 characters");
																				document.getElementById("password").value = "";
                                                                                 return false;  
													                     } 
													   if(pwd.length > 64){
														                        alert("Error : Password length must be maximum 64 characters");
																		N		document.getElementById("password").value = "";
                                                                                 return false;  
													                     } 
									                  var pattern = new RegExp(/[~`!#$%\^&*+=\-\[\]\\';,/{}|\\":<>\?]/); 
                                                      if (!pattern.test(pwd)) {
                                                                                 alert("Error : Your password must have atleast one special character \n like : !#$+*&");
																				 document.getElementById("password").value = "";
                                                                                 return false;
                                                                                }
                                                      var capitalPattern = new RegExp(/[A-Z]/);
													  if(!capitalPattern.test(pwd)){
														                           alert("Error : Your password must be one capital letter");
																				   document.getElementById("password").value = "";
														                           return false;
													                               }
									                  var digitPattern = new RegExp(/[0-9]/);
													  if(!digitPattern.test(pwd)){
														                           alert("Error : Your password must be contain one digit ");
																				   document.getElementById("password").value = "";
														                           return false;
													                               }
					                                  }					
				</script>
			</form>
		</div>
	</div>
</body>
</html>
